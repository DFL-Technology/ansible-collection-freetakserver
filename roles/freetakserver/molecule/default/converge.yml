---
- name: Converge
  hosts: all
  gather_facts: yes
  tasks:
    - name: Get IPv4
      debug:
        var: ansible_default_ipv4
        verbosity: 0
    - name: Include freetakserver
      include_role:
        name: freetakserver
